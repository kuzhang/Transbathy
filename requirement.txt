pip3 install torch torchvision torchaudio #cpu version
pip install geopandas
pip install openpyxl
pip install pandas
pip install matplotlib
pip install numpy
pip install rasterio
pip install PyYAML
pip install tqdm
pip install scipy
pip install rioxarray

###optional packages###
##GDAL
# linux
conda install -c conda-forge gdal
# windows
# download gdal python wheel

## data smoothing
pip install opencv-python


## sentinellat package
pip install sentinelsat
git clone https://github.com/sentinelsat/sentinelsat.git
cd sentinelsat
pip install -e .[dev]
pytest -v

## world map
conda install -c conda-forge pygal
